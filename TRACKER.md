# TRACKER — Squatter

Single source of truth for build progress. Update on every meaningful change.
Requirements and priorities: [PROJECT_SPEC.md](PROJECT_SPEC.md).
Mirrored in Linear: [Squatter project](https://linear.app/ielyas/project/squatter-8016284756f8) — phases = milestones, tasks = `E2-…` issues (see [rules/issue-tracker-status.md](rules/issue-tracker-status.md)).

## Phase board

| Phase | Scope | Status |
|-------|-------|--------|
| M0 | Foundation — spec, rules, Xcode project scaffold, `.gitignore`, CI skeleton | ✅ Done (2026-08-27) |
| M1 | P0 core — `LsofParser` + tests, `PortScanner` actor, `MenuBarExtra` list, open/copy/kill, refresh, empty/error states, Launch at Login, filter | ✅ Done (2026-08-28) |
| M2 | P1 polish — ignore list, sort, greyed root-owned rows, keyboard nav, menu bar count | ✅ Done (2026-08-28) |
| M3 | Release — Developer ID signing, notarization, GitHub Release, README | 🔄 In progress |

Status legend: ⬜ Not started · 🔄 In progress · ✅ Done

## M0 tasks

- [x] `PROJECT_SPEC.md` written — [E2-140](https://linear.app/ielyas/issue/E2-140) (In Review)
- [x] Agent rules (`AGENTS.md`, `CLAUDE.md`, `rules/`) and tracking docs created — [E2-141](https://linear.app/ielyas/issue/E2-141) (In Review)
- [x] Repo initialised and published: [github.com/National-Idea-LLC/squatter](https://github.com/National-Idea-LLC/squatter) (public, MIT) — [E2-159](https://linear.app/ielyas/issue/E2-159) (In Review)
- [x] Resolve spec open questions #1–#3 — app / macOS 15 / row click selects, ↗ opens — [E2-142](https://linear.app/ielyas/issue/E2-142) (In Review)
- [x] Create `Squatter.xcodeproj` via `project.yml` + xcodegen (app + test targets, Swift 6 strict, macOS 15, `LSUIElement`, sandbox off, hardened runtime on) — [E2-143](https://linear.app/ielyas/issue/E2-143) (In Review)
- [x] Capture `lsof` fixture into `SquatterTests/Fixtures/lsof-sample.txt` — [E2-144](https://linear.app/ielyas/issue/E2-144) (In Review)
- [x] GitHub Actions: `xcodebuild test` on PR and push to `main` (`.github/workflows/ci.yml`, `macos-26`, ad-hoc signing) — [E2-145](https://linear.app/ielyas/issue/E2-145) (In Review)

## M1 tasks

- [x] `LsofParser` pure parser + fixture/edge-case tests — [E2-164](https://linear.app/ielyas/issue/E2-164) (In Review)
- [x] `LsofRunner` (`Process` wrapper, fixed args, exit-code contract) + `PortScanner` actor (coalesced refresh) — [E2-166](https://linear.app/ielyas/issue/E2-166) (In Review)
- [x] `ProcessKiller` — SIGTERM, PID re-validation, Force Kill (SIGKILL) after ~2 s — [E2-168](https://linear.app/ielyas/issue/E2-168) (In Review)
- [x] `PortListModel` (`@MainActor @Observable`) — poll every 2 s while popover visible, filter, kill flow — [E2-169](https://linear.app/ielyas/issue/E2-169) (In Review)
- [x] `PortListView` / `PortRow` — list, selection, ↗ open, copy URL/port/PID, kill, context menu — [E2-176](https://linear.app/ielyas/issue/E2-176) (In Review)
- [x] Empty and error states — E2-176
- [x] Launch at Login (`SMAppService`) + settings popover (refresh interval) + ⌘Q — [E2-179](https://linear.app/ielyas/issue/E2-179) (In Review)

## M2 tasks

- [x] Ignore list — hide ports/processes, "N hidden · Show" footer, manage in Settings — [E2-180](https://linear.app/ielyas/issue/E2-180) (In Review)
- [x] Sort by port / process name (Settings picker, persisted) — [E2-181](https://linear.app/ielyas/issue/E2-181) (In Review)
- [x] Keyboard: ⌫ kills selected row, ⌘C copies URL, list focused on open — [E2-182](https://linear.app/ielyas/issue/E2-182) (In Review)
- [x] Menu bar count badge (opt-in, 10 s background poll while closed) — [E2-183](https://linear.app/ielyas/issue/E2-183) (In Review)
- [x] "Check for Updates" → GitHub Releases, View Source, version row — [E2-184](https://linear.app/ielyas/issue/E2-184) (In Review)
- [x] Greyed rows for other users' processes — shipped in M1 (E2-176)
- [x] Row options button (⋯) presenting the same menu as right-click — [E2-186](https://linear.app/ielyas/issue/E2-186) (In Review)
- [x] Popover redesign after first-launch feedback — status LEDs, Yours / Other users / Ignored groups, port-first rows — [E2-185](https://linear.app/ielyas/issue/E2-185) (In Review)

## M3 tasks

- [x] Rename to Squatter — [E2-187](https://linear.app/ielyas/issue/E2-187) (In Review)
- [x] App icon, `scripts/release.sh`, DMG, Homebrew cask skeleton — [E2-188](https://linear.app/ielyas/issue/E2-188) (In Review)
- [x] Notarization credentials stored (keychain profile `squatter`)
- [x] Notarize + staple — `spctl` reports `accepted / source=Notarized Developer ID`
- [x] README screenshots + install instructions
- [x] Tag `v0.1.0`, move CHANGELOG `[Unreleased]` → `[0.1.0]`, publish GitHub Release with the DMG — shipped, and v0.1.1, v0.1.2 and v0.2.0 since
- [x] Publish the Homebrew cask — [`National-Idea-LLC/homebrew-tap`](https://github.com/National-Idea-LLC/homebrew-tap) is live and is the only copy of the cask — [E2-219](https://linear.app/ielyas/issue/E2-219) (In Review)
- [x] Docker: name the container behind a published port instead of `com.docker.backend` (plan 007) — [E2-220](https://linear.app/ielyas/issue/E2-220) (In Review)
- [x] Docker: offer Stop Container instead of killing Docker's host proxy (plan 008) — [E2-221](https://linear.app/ielyas/issue/E2-221) (In Review)
- [x] Hide ports above a configurable threshold, off by default (plan 009) — [E2-222](https://linear.app/ielyas/issue/E2-222) (In Review)
- [x] Type ports straight into the ignore list (plan 010) — [E2-223](https://linear.app/ielyas/issue/E2-223) (In Review)
- [x] "Report a Bug" link in Settings (plan 011) — [E2-224](https://linear.app/ielyas/issue/E2-224) (In Review)
- [x] Settings: ignore list before About — [E2-225](https://linear.app/ielyas/issue/E2-225) (In Review)
- [x] Snapshot tests assert rendered dimensions, so they can fail (plan 012) — [E2-226](https://linear.app/ielyas/issue/E2-226) (In Review)
- [x] Liquid Glass on macOS 26+, material fallback below — [E2-228](https://linear.app/ielyas/issue/E2-228) (In Review)
- [x] Refresh icon only spins for user-initiated refreshes — [E2-229](https://linear.app/ielyas/issue/E2-229) (In Review)
- [x] Settings readable: footer contrast lifted, orphan sentences given a subject (the larger labels were reverted before shipping) — [E2-230](https://linear.app/ielyas/issue/E2-230) (In Review)
- [x] App icon replaced with the approved Icon Composer design, shipped as `AppIcon.icon` — [E2-231](https://linear.app/ielyas/issue/E2-231) (In Review)
- [x] Row button tooltips appear fast enough to read — [E2-232](https://linear.app/ielyas/issue/E2-232) (In Review)
- [x] Each row shows its URL on its own line, clickable — [E2-233](https://linear.app/ielyas/issue/E2-233) (In Review)
- [x] Row URL legible on glass, and README screenshots recaptured per appearance — [E2-234](https://linear.app/ielyas/issue/E2-234) (In Review)
- [x] Create the `TAP_TOKEN` repository secret so the cask-bump workflow can actually run — created by the owner 2026-08-29 and proven with the `verify_auth` probe; a real end-to-end bump still awaits the next release — [E2-227](https://linear.app/ielyas/issue/E2-227) (In Review)
- [ ] Submit to `homebrew/cask` core so `brew install --cask squatter` needs no tap — blocked on Homebrew's notability requirements (~75 stars / 30 forks)
- [x] Manual check: Launch at Login toggle against the signed build — confirmed by the owner 2026-08-29
- [x] Manual check: run Squatter with Docker Desktop actually running — verified 2026-08-29 against Docker Desktop 4.88.1 / engine 29.7.2. The parser's premises held: raw `docker ps --no-trunc --format '{{json .}}'` bytes contain `->` (never a literal `->`), the separator is exactly `, `, ranges look like `0.0.0.0:9000-9002->9000-9002/tcp`, host≠container works, IPv6 appears as `[::]:5432->5432/tcp`, `State` is the bare string `running`, and every published port really does show up in `lsof` as one `com.docker.backend` PID under uid 501 — confirming the problem 007 solves and that the ✕ button really was enabled on all of them before 008. The one real defect real-daemon testing caught: `docker stop --time 5` is deprecated on engine 29.7.2 (prints the warning to stdout); fixed by switching to `-t`, the one spelling that has been valid since Docker 1.x and works on both old and new engines. The synthetic fixture's one wrong detail — an exposed-but-unpublished port appearing alongside a container's real publishes — was replaced with real captured output.
- [x] Manual check: stop a real container through the Squatter UI (plan 008) — confirmed by the owner 2026-08-29. This was the last unverified link in the Docker chain: `docker stop` itself had already been run against a real daemon from the CLI (Docker Desktop 4.88.1, port released in 0.22 s, see the box above), but the SwiftUI wiring had only ever been exercised by tests and offscreen snapshots.
- [x] Updates via Sparkle (plan 013) — [E2-243](https://linear.app/ielyas/issue/E2-243) (In Review)
- [x] Settings: Ignored section redesigned — full-width port field, ignored entries as chips — [E2-244](https://linear.app/ielyas/issue/E2-244) (In Review)

## Dev changelog

<!-- Newest first. One dated entry per meaningful change. -->

- **2026-08-30** — Redesigned Settings' **Ignored** section (owner: "redesign this section of the settings, use $better-ui and $better-layout"). The screenshot showed the actual bug before any principle did: the example ports were passed as the `TextField`'s **title**, not its `prompt`, and a title inside `LabeledContent` renders as static text *beside* the box — so "3000, 5173" read as two ports already ignored rather than as a hint. Fixed with `prompt:` + a hidden label the field still carries for VoiceOver. Everything else followed from the width: a leading label plus a 120 pt fixed field in a 320 pt panel left a box too narrow to show its own example, let alone paste a list into, so label / field / hint now stack on one leading edge and the field takes the full width. The icon-only "+" went back to a titled **Add Ports** — it was the only icon-only action in a Form where Open Login Items, Check for Updates and Report a Bug all carry verbs — and the syntax hint moved out of the section footer to sit directly under the field, which is the same "orphan sentence with nothing on screen to attach to" the footer two sections up was already rewritten to fix. The ignored entries are **chips now, not one Form row each**: eight ignored ports were eight full-width rows in a 480 pt panel, each repeating the word "Port" inside a section already headed "Ignored". They are split under "Ports" and "Processes" captions at 12 pt between groups against 6 pt inside one (twice the inner gap is what makes them read as two groups, so no rule is drawn between them) — a bare `3000` and a bare `node` side by side otherwise gave no way to tell which kind of rule you were about to remove. Each chip is entirely the button with the ✕ as its affordance, since a 12 pt glyph inside a 24 pt chip would be the only hittable part; the ✕ stays **neutral, not red** — stopping an ignore restores a row rather than destroying anything — and reuses `PortRow`'s dismiss weight, glass chip shape and 0.12 s hover fade so the panel and the list read as one vocabulary. New `WrapLayout` (`Layout`) does the packing: `LazyVGrid`'s adaptive columns force every cell to a shared width, which is wrong for chips whose width is their text. It takes `layoutDirection` as a property because `Layout` places subviews in raw coordinates that SwiftUI does not mirror for it, and it clamps any subview wider than the line so a long process name truncates *inside* its chip instead of running off the panel. Added an empty state ("Nothing is ignored yet.") — the section previously showed nothing at all when the list was empty. Build and 135 tests green; relaunched via `scripts/run-debug.sh` per golden rule 9, since glass chips do not render offscreen (E2-244).

- **2026-08-30** — **Released 0.4.0** (build 5), the first build that can be updated *to* through Sparkle. `scripts/release.sh --notarize` ran clean end to end: 135 tests, app and DMG both `Accepted` by the notary service and stapled, the new public-key equality check passed (the app's `SUPublicEDKey` equals the Keychain's `ed25519` key), and `generate_appcast` 2.9.6 wrote a signed `appcast.xml` with the 0.4.0 CHANGELOG section embedded as markdown. Tag `v0.4.0` pushed; GitHub Release published with exactly two assets, `Squatter-0.4.0.dmg` (sha256 `e1dff7b0…`, verified equal between the local file and the published download) and `appcast.xml`. `releases/latest/download/appcast.xml` redirects to the v0.4.0 copy and carries one `sparkle:edSignature` — that URL is what every 0.4.0 copy will poll. **The cask workflow passed its new guard on the first real run** (run 33309761995: "Verified: appcast present, signed, points at Squatter-0.4.0.dmg") and merged the bump into the tap as `962356e`; `brew info --cask national-idea-llc/tap/squatter` reports `0.4.0 (auto_updates)` with the same sha256. Still unproven, and unprovable until there is something newer to offer: a 0.4.0 install being offered 0.4.1 in place — that is the next release's job. Linear: E2-243 commented with the release, project description's constraints and status line updated (E2-243).
- **2026-08-30** — Cut **0.4.0**: `MARKETING_VERSION` 0.3.0 → 0.4.0, `CURRENT_PROJECT_VERSION` 4 → 5, project regenerated from `project.yml`, and `CHANGELOG.md`'s `[Unreleased]` block moved under `## [0.4.0]` with a one-sentence summary. A minor because Sparkle is new capability: this is the first build that carries an appcast and therefore the first one users can update *to*, and the last one anyone installs by hand. Both numbers had to move — Sparkle compares `CFBundleVersion`, so a reused build number would have been invisible to installed copies. Same-day owner follow-ups closed before the cut: the private key exported to a file outside the repo for the password manager (its public half confirmed equal to `SUPublicEDKey`), and `auto_updates true` pushed to the tap as a verified-signature commit (`da5c39a`) — `brew info` now reports `0.3.0 (auto_updates)`, so `brew upgrade` will leave the app to Sparkle. Plan 013 filed as E2-243 (M3, In Review) (E2-243).
- **2026-08-30** — Squatter updates itself through Sparkle 2.9.6, pinned with `exactVersion` in `project.yml` (owner: "add Sparkle for auto updates", plan 013). This is a deliberate exception to "no third-party packages" and "no network calls" — every place those principles were written down (README, AGENTS.md, PROJECT_SPEC.md, `rules/ios-swift.md`, `rules/secrets-and-config.md`) was rewritten in the same change so the docs stop contradicting the code. The appcast is a release asset (`releases/latest/download/appcast.xml`, GitHub's own redirect), signed with an EdDSA key from Sparkle's `generate_keys`; the owner chose to **share the existing key already in the login Keychain** (account `ed25519`) rather than mint a second one, following Sparkle's one-key-per-organisation guidance — it already signs GhostCursor's releases. The update archive is the existing signed, notarized, stapled DMG (no zip, no deltas). Automatic checks are opt-in: `SUEnableAutomaticChecks` is left unset so Sparkle asks the user on the second launch, mirrored by a Settings toggle; `SUEnableSystemProfiling` is explicitly off. Squatter is `LSUIElement` with no windows, so a scheduled check that finds an update while the popover is closed shows nothing — Sparkle 2.2+'s gentle reminders hold the found version (`SparkleUpdater.pendingUpdateVersion`) instead of opening an alert behind everything; a dot appears on the footer gear and Settings › About offers Install Update, which is a normal user-initiated check and so Sparkle presents it in focus. The updater sits behind an `UpdateChecking` protocol, modelled on `LoginItemManaging`, and is never started under XCTest (`XCTestConfigurationFilePath`) since the test host shares the app's defaults. `scripts/release.sh` now also builds a signed `appcast.xml` after stapling (stapling changes the DMG's bytes, so signing must happen after) and refuses to proceed if the app's `SUPublicEDKey` does not match the Keychain's signing key, or if `generate_appcast` produces an item with no `sparkle:edSignature` — both would otherwise silently ship a build that can never update or an unsigned feed. `cask.yml`'s bump job now also refuses to advertise a release with no appcast, an unsigned one, or one that does not point at the release's own DMG. 135 tests green (up from 130). **The first release carrying an appcast is the first one users can update *to*** — everyone on 0.3.0 or earlier updates by hand (or `brew upgrade`) one last time before Sparkle can take over.

- **2026-08-30** — Settings' About links are rows now, not labels with buttons parked at the trailing edge (owner: "this rows should be clickable by themselves with arrow.up.right icon"). New private `LinkRow` in `SettingsView`: `Button` + `LabeledContent` + a trailing `arrow.up.right`, `.buttonStyle(.plain)`, pointing-hand cursor and the arrow lifting 0.6 → 1 on hover, reusing the pattern `URLLine` already established in `PortRow`. `.contentShape(Rectangle())` is what makes it work — without it the hit area is the text and the glyph but not the gap between them, which in a 320 pt panel is most of the row. **Applied to all four About links first and then rolled back to two** (owner: "revert updates and bugs to the way they were, just the github and website"), which lands on a better rule than the uniformity I had argued for: Check for Updates and Report a Bug *do* something on your behalf — one checks, the other opens an issue form pre-filled with your versions — so the verb belongs on a control, while source and the company credit are plain destinations. That distinction is written at the call site so it does not get "tidied" back into consistency later. Accessibility needed care, since the trade is a visible verb for a visible arrow: each row is one element labelled by its own text ("Open source, MIT") with the verb as a named action ("View Source"), so VoiceOver still announces something you can be told to do. Not snapshot-testable — hover and cursor do not render offscreen (E2-242).

- **2026-08-30** — Sort by PID, a third `SortOrder` alongside port and process name (owner: "add sort by id" — read as the PID, the only id a row shows; `Listener.id` is itself just `"pid:port"`). Ascending PID with **port** as the tie-break, not name: one process can hold several ports — Vite and its HMR port — and without the tie-break those come out in whatever order the scan produced. Labelled `PID` in the picker to match the row's own `PID 87489` rather than inventing a second word for it. Adding a case to a `String`-backed `CaseIterable` enum is safe in both directions here: `Preferences.sortOrder` already falls back to `.port` on an unreadable value, which is the downgrade path if someone opens an older build after selecting this — covered by the existing `sortOrderFallsBackToPortOnUnknownValue`. The new test pins numeric ordering (12 → 120 → 900, not the string order) and the within-PID port tie-break. 130 tests green (E2-241).

- **2026-08-30** — Company credit in Settings' About section (owner: "add company credit to the app info, © [CURRENT_YEAR] National Idea LLC https://ni.sa"). The year is **static, not `Date()`** — a copyright year is the year the build was published, so a copy shipped in 2026 must not claim 2029 on someone's Mac in 2029. It comes from `NSHumanReadableCopyright` via a new `SettingsModel.bundleCopyright`, so the string in the About row and the one Finder shows in Get Info cannot drift; bumping the year is one line in `project.yml`. Dropped the trailing ". MIT License." from that key, since MIT has its own row directly above the credit and the plist string is now displayed verbatim. `openCompany()` goes through the existing `SystemActions` seam like every other link, so the app still never makes a request itself. The credit test matches `© \\d{4} National Idea LLC` **by shape, not by literal**: an exact-string assertion would have to be edited every time the year is bumped, at which point it asserts nothing that the plist does not already say. 129 tests green (E2-240).

- **2026-08-30** — Tuned the two new symbol animations and added a third (owner: "animation speed is too slow", then "make the refresh icon faster, 0.5", then "same for settings icon"). The rotation was the real complaint and the cause was arithmetic I had not checked: SwiftUI's continuous rotate runs about a second per turn, so the 400 ms hold was showing roughly a **third** of a revolution, slowly — the opposite of the "one clean turn" the hold was introduced for. Now `.speed(5)` against a 200 ms hold, which is one turn; the pairing is documented in both files because changing either alone reintroduces the partial-turn bug. "0.5" was read as half the duration, since the two readings that take 0.5 literally (a 0.5 s turn, or `.speed(0.5)`) are both *slower* than what was already there — flagged in the reply rather than silently chosen. The eye's replace also went to `.easeInOut(duration: 0.2)` from `.default`'s ~0.35 s. The gear is **bounce, not rotate**: per the HIG it is acknowledging a click rather than reporting work, and it fires on close as well as open since the popover hangs below the button and leaves the gear visible. Bounce keeps its stock speed on purpose — its elastic timing is the thing that makes it read as a bounce (E2-239).

- **2026-08-29** — The footer's refresh arrow rotates while it scans (owner: "do the same for the refresh icon", with a Rotate / By Layer / Default-direction screenshot). Unlike the eye, this one could not just take an effect modifier: the button **already had** an indicator, swapping the arrow for a `ProgressView` while `isRefreshing`, so an animated arrow and the spinner would have been two indicators competing for one 13 pt slot. Asked rather than guessed, and the owner chose the arrow *as* the indicator — `.symbolEffect(.rotate, options: .repeat(.continuous), isActive: model.isRefreshing)`, `ProgressView` deleted. That is also the HIG's own example for Rotate ("when a task is in progress, rotation confirms that it's working as expected"), and it drops a width change under the button, since the spinner and the glyph did not measure the same. **The interesting half is in the model, not the view**: a local `lsof` scan returns in well under 100 ms, so a rotation bound honestly to the scan's duration would stop a fraction of a turn in and read as a twitch. `minimumRefreshDisplay` (injectable alongside `forceKillGrace`/`badgeInterval`) holds `isRefreshing` open (200 ms, paired with a 5x rotation speed — see the tuning entry above), and the hold is placed **after** every state update in `scan` — the rows, counts and error are on screen immediately and only the arrow is still turning, so this delays feedback, never data. A slow scan overruns the hold and keeps spinning on its own; `lsof`'s ceiling is 10 s (`ProcessRunner.defaultTimeout`), which is exactly the case that made dropping the indicator for a one-shot turn the wrong option. The hold is gated on `showingProgress`, so background polls skip it — otherwise a hold longer than the poll interval would throttle the list to the indicator's pace for an indicator nobody is showing; that is the second of the two new tests, and it is the one that would catch a real regression. Tests inject `.zero` by default so the suite does not pay the hold on every refresh. 128 tests green (E2-238).

- **2026-08-29** — The footer's ignore toggle animates its icon (owner: "let's animate the ignore icon when hiding/showing ignored ports", pointing at the SF Symbols HIG). `eye`/`eye.slash` is Apple's own worked example of a **Magic Replace** pair — related shapes, where the slash draws on and off rather than one glyph cross-fading into another — so this is `.contentTransition(.symbolEffect(.replace.magic(fallback: .upUp)))`. The fallback is unreachable for this pair (magic replace only declines between *unrelated* symbols) and exists because `.magic` requires one; `.upUp` matches what the owner had selected in the SF Symbols app. No availability gate needed — `.replace.magic` is macOS 15.0 and the deployment target is 15.0. **The animation is scoped to the `Image`, not the toggle**: `.animation(.default, value: model.showIgnored)` rather than wrapping `model.showIgnored.toggle()` in `withAnimation`, because the same state drives `filtered`, so an animated transaction there would also slide every ignored row into the `List` behind it — a much larger motion than the one that was asked for, triggered by a button whose whole job is to be unobtrusive. Deliberately left alone: the `"N ignored"` count next to it does not get `.numericText()`, and the `eye`/`eye.slash` icons in the row context menu stay static — per the HIG's "apply symbol animations judiciously", one animated symbol in the footer reads as feedback, four read as a twitchy UI. Untestable in the suite for the usual reason: the snapshot harness captures a single frame via `cacheDisplay(in:to:)`, so a symbol effect either renders as its end state or not at all, which is why this went straight to a relaunch under the new golden rule 9. 126 tests green (E2-237).

- **2026-08-29** — Agents now relaunch the app after a UI change instead of leaving the owner to build it themselves (owner: "when i ask you to make a change to the ui, you should relaunch the app once finished so i can see the changes"). AGENTS.md golden rule 9, backed by `scripts/run-debug.sh` so the rule is one command rather than a paragraph of `xcodebuild` — it builds Debug, resolves `BUILT_PRODUCTS_DIR` from `-showBuildSettings` rather than hardcoding the DerivedData hash, `pkill -x Squatter` and waits up to 2 s for it to actually exit, then `open`s the fresh bundle. The quit step is the part that matters: two copies means two menu bar icons and the stale one keeps scanning every 2 s, which looks exactly like a bug in the change you just made. Rule says snapshots are not a substitute, and that is not boilerplate — the 2026-08-29 README work established that `glassSurface` renders near-invisible under `cacheDisplay(in:to:)` because there is no backdrop to refract, so a green snapshot suite can coexist with a visibly broken panel. Scoped to visible changes (layout, copy, icons, colour, animation, Settings) and explicitly *not* refactors/CI/docs, so it does not turn every commit into a relaunch. Note `LSUIElement: true`: the relaunched app shows no window and no Dock icon, only the menu bar item, so "launched" in the script's output is the whole confirmation available from the terminal — an agent still cannot see the result, only put it in front of the owner (E2-236).

- **2026-08-29** — The footer's Refresh button is icon-only now (owner: "let's remove numbers from the refresh icon"). The `"7 of 17"` / `"17 listening"` label sat inside the button's own `HStack`, so it read as part of the control rather than as a status line, and on the Liquid Glass footer it made the leftmost pill twice the width of every other one. The count itself is not deleted: `countText` still exists and now feeds `.accessibilityLabel` ("Refresh list, 7 of 17") and `.help` ("Refresh (⌘R) — 7 of 17"), so VoiceOver and the tooltip keep it, and the menu bar's own count — a separate path in `SquatterApp.swift` — is untouched. The `"N ignored"` pill next to it is deliberately left alone: it is a toggle whose number *is* its reason to exist, and removing it would leave an eye icon with nothing to say how much is hidden. Note the loss this does accept: with the filter field non-empty there is now no on-screen sign that the list is a subset — the tooltip only says so on hover. Worth watching if filtering ever starts to feel silent. 126 tests green (E2-235).

- **2026-08-29** — Row URLs are legible again, and the README finally shows the app as it actually looks (owner: "in light mode, the link's color is not clear"). The URL line shipped in 0.3.0 using `.tint`, which was the wrong choice for a translucent surface and is now reverted: on Liquid Glass the panel's luminance comes from whatever is *behind* the window, so an accent blue that reads fine on white washes out over a dark wallpaper, and there is no way to sample the backdrop and compensate. It now uses `.primary` — the colour the process name directly above it already uses, and visibly legible in the owner's own screenshot — with a **permanent** underline carrying the affordance instead of the colour, and hover moving opacity 0.75 → 1 so there is still feedback. That reverses my own call from earlier the same day that a permanent underline would turn the list into "a wall of rules"; it would, mildly, and legibility wins. Confirmed by the owner against a real wallpaper in both appearances. **The README screenshot could not be refreshed from the snapshot suite**, which is how it was made at v0.1.0: `glassSurface` renders near-invisible offscreen because there is no backdrop to refract, so a regenerated shot showed a bare search field and no footer chrome — the app looking *unstyled*, worse than the pre-Glass image it would replace. Same root cause as the unreadable `list-dark.png`, which had been tracked as a separate bug and is not one: anything depending on what is behind the window renders wrong in `cacheDisplay(in:to:)`. So the shots are now real captures supplied by the owner, one per appearance, wired up with a `<picture>` element so GitHub serves the right one per reader's theme. Checked them for private content before committing: no username, email, hostname, path, LAN or public IP — every address is `127.0.0.1`, `::1` or `*` — with one judgement call flagged to the owner rather than decided for them, that "T3 Code (Alpha)" advertises an unreleased tool. Known and accepted: the glass footer overlaps the last row's URL, which is real behaviour (the list scrolls under it) but reads as a glitch in a still. Also dropped `docs/screenshot-settings.png`, an 82 KB orphan referenced nowhere since v0.1.0, and corrected the README status line, which still claimed "v0.1.0 — first public release" three lines above the image. 126 tests green (E2-234).

- **2026-08-29** — `TAP_TOKEN` exists at last (owner created the fine-grained PAT; resource owner `National-Idea-LLC`, not the personal account, which is the step that silently fails for an org-owned tap) — and, more usefully, the cask workflow can now **prove** its own credential. The first dispatch after the token landed went green while testing nothing: the cask was already at 0.2.0, so the bump job hit `Cask is already at 0.2.0 — nothing to do` and exited before any write, and the `git clone` ahead of it succeeded regardless because the tap is **public**. That pair is precisely what hid the missing secret across two releases — a green run and a stale cask are indistinguishable from outside — so it was worth not accepting the green. `verify_auth: true` (new `workflow_dispatch` input, commit `639dc16`) exercises both permissions for real against the live tap: push a branch (Contents: write), open a PR (Pull requests: write), then **close** the PR rather than merging and delete the branch. Run 33269136292 printed both OK markers; verified afterwards from outside the run that tap `main` was still `483563c`, the cask still `version "0.2.0"`, probe PR #3 `CLOSED / mergedAt: never`, and zero `auth-probe` branches left behind. The probe writes only `.tap-auth-probe`, never `Casks/`, so a bug in it cannot produce a shippable-looking cask change, and cleanup runs under `if: always()` so a partial failure cannot strand a branch or an open PR on a public repo. `tag` became optional, since it is meaningless for a probe run and the resolve step already rejects an empty one. **Still unproven:** a real version-changing bump end to end — the probe covers the credential, not the bump path, and 0.3.0 is the first run that will exercise it. **Known future failure:** the token carries an expiry, and on that date this breaks again in the same silent way; the probe makes that checkable, not self-healing (E2-227).

- **2026-08-29** — Reverted the 15 pt settings labels back to the Form's own size (owner: "labels text sizes are too large, bring back the default"). This undoes the second half of the 2026-08-29 readability work below, which had gone the other way on "the labels are too small" — the two rounds bracket the answer, and the Form's default is where it lands. `settingsLabel()` is deleted rather than emptied, along with all thirteen call sites; leaving a no-op modifier in place would have invited someone to "restore" it. The call sites keep their explicit `label:` closures, which are behaviourally identical to the string-label convenience initialisers they replaced, so reverting those too would have been diff for its own sake. **`settingsFooter()` stays** — the owner objected to label size only, and the footer work was a separate contrast fix (~3.9:1 → ~6.4:1) plus a copy fix for the orphan sentences. Labels and values now render at the same size again, which reads as ordinary label/value parity rather than hierarchy. Nothing user-facing to log as a reversal: the 15 pt labels only ever existed under `[Unreleased]`, so that CHANGELOG bullet was amended in place instead. The comment above the extension now records that 15 pt was tried and rejected, so the next person reading "the labels are too small" does not retry it. 126 tests green (E2-230).

- **2026-08-29** — Each row now spells out its URL on its own line, clickable, rather than hiding it in the ↗ button's tooltip (owner: "the link should be visible in the row too"). Flagged before building that `Listener.localURLString` is hardcoded to `"http://localhost:\(port)"` and ignores the bind address, so the line carries no information the row does not already show — the port is right there at `title3` — and a row bound to `0.0.0.0` still links to localhost. Owner's call on both: show the full URL and keep everything else (a third line, accepting taller rows and fewer visible in the 460 pt panel), and leave the localhost behaviour alone, since on a dev machine localhost reaches every locally-bound port whatever the row advertises. **First attempt was wrong and the snapshot caught it**: putting `URLLine` inside `details` left it sharing the `HStack` with the hover chips, so the row being hovered — the only one whose URL you are actually reading — truncated to `http://lo…host:3000` while every row at rest showed in full. Fixed by lifting the whole row into a `VStack` and giving the URL the full width *below* the chips rather than beside them, with the 89 pt text-column inset (LED 7 + 10 + port 62 + 10) named as `textColumnInset` and its arithmetic written down, since it has to track the `HStack` above it by hand. The line is hidden while a row is confirming a kill/force-kill/stop, so the prompt and its buttons keep the space they already had. `.accessibilityHidden(true)` on it: the row is a single `accessibilityElement(children: .ignore)` with an "Open in Browser" action already, and a second near-duplicate target would be noise. Underline on hover only, plus `NSCursor.pointingHand` — a permanently underlined line in every row turns the list into a wall of rules. Separately noted while eyeballing, **pre-existing and not from this change**: `list-dark.png` is unreadable, because the harness sets `window.appearance = .darkAqua` but `cacheDisplay(in:to:)` does not capture the window background, so white text composites onto a transparent backing. The light renders only look right because their text is dark. The suite still passes — it asserts frame dimensions and a >1 KB byte floor, neither of which this trips. Worth fixing before anyone trusts a dark snapshot. 126 tests green (E2-233).

- **2026-08-29** — Row button tooltips now appear after 400 ms instead of AppKit's ~2 s (owner: "2 seconds is too long for the action buttons"). Worth recording what this *wasn't*: the request arrived as "let's add tooltips to the buttons", but every control already had one — `.help()` on the ⋯, ↗ and ✕ chips, on the footer's Refresh/Settings/Quit, on the address chip and the Docker badge. Adding more would have been a no-op. Two wrong theories died before the right one: that `MenuBarExtra(.window)`'s non-activating panel suppressed tooltips outright (it doesn't — the footer's fire normally), and that the 2 s poll was churning row identity and resetting the tooltip timer (it isn't — `Listener.id` is `"pid:port"`, stable across scans). The actual cause is the interaction between the stock delay and `trailing`'s `if isHovering || isSelected` gate at `PortRow.swift:143`: the chips are only mounted while the row is hovered, so the two seconds routinely outlast the time the pointer spends over any one of them, and the tip lands after you have moved on — or never. Fixed by registering `NSInitialToolTipDelay` (AppKit's own key, hence no `squatter.` prefix — `DefaultsKeys.initialToolTipDelay` carries a comment saying so, and `keysCarryThePrefix` now asserts the exception on purpose so nobody "fixes" it). **Registered, not set**, via `UserDefaults.register(defaults:)` in `SquatterApp.init()`: the value stays in the registration domain, so it never lands in the user's plist and an explicit `defaults write` of the same key still wins — covered by `toolTipDelayDoesNotOverrideAnExplicitUserSetting`. One test premise was wrong on the first run and is worth keeping in mind: the registration domain is **process-wide**, consulted by every `UserDefaults` instance including a fresh suite, so a "key is unset beforehand" precondition cannot hold in a test hosted by the app — the app has already registered it at launch. App-wide rather than scoped to the row chips, which also speeds the footer tips; that is the smaller change and reads as consistent. Timing itself is not assertable in a unit test, so 400 ms wants the owner's eye. 126 tests green (E2-232).

- **2026-08-29** — Adopted the approved Icon Composer app icon (owner approved "Draft 1"). The design source now lives in `brand/app_icon/`, and the app ships the layered `.icon` itself rather than a rendered PNG set: `Squatter/Resources/AppIcon.icon` replaces the ten-PNG `AppIcon.appiconset`, and `scripts/make-icon.swift` — the hand-coded CoreGraphics generator that drew the old icon — is deleted. `ASSETCATALOG_COMPILER_APPICON_NAME` was already `AppIcon`, so naming the bundle `AppIcon.icon` needed no build-setting change; XcodeGen 2.46 already knows the extension and references it as an opaque `wrapper.icon` rather than recursing into its SVGs. **The Draft 1 export does not build as authored**, and the failure is worth writing down because it is silent: `actool` aborts with `Exception while running actool: *** -[__NSPlaceholderArray initWithObjects:count:]: attempt to insert nil object from objects[0]` and no file or line. Bisecting `icon.json` field by field found two newer-schema keys that Xcode 26.6's `actool` cannot parse — top-level `"features": ["refractivity", "specular-location"]`, and per-group `"specular": "outside"` where a boolean is expected (**any** string value crashes it; `true`, `false` and omission all compile). Both must go together: normalising either one alone still crashes, which is why a single-field fix looks like a dead end. Not platform-specific — it crashes identically for `iphoneos`, and identically at deployment target 26.0, so this is a schema mismatch rather than the macOS 15 target. `scripts/sync-app-icon.sh` now does the copy-and-normalise so a re-export from Icon Composer cannot quietly reintroduce it, and `AGENTS.md` documents it in place of the retired generator. Checked rather than assumed: the shipped `Assets.car` carries two 1024×1024 representations and `CFBundleIconName`/`CFBundleIconFile` are both `AppIcon`, so the 256 px ceiling on the generated legacy `AppIcon.icns` (`ic04`/`ic11`/`ic07`/`ic13` only) is a deeper fallback, not the icon macOS actually draws — no resolution regression against the old 1024 px set. Also note the six PNGs under `Exports/Draft 1/` are **iOS** renders: 1024², squircle mask baked in, filling the frame. Dropping those into a `mac`-idiom appiconset would have shipped an icon visibly oversized in the Dock, since macOS icons sit inset within their canvas; shipping the `.icon` avoids this because actool insets it correctly, confirmed in the extracted `icon_128x128@2x.png`. Per the owner's call, `brand/` is committed but `brand/**/Exports/` is gitignored — the exports are 4.6 MB of the 5.6 MB and are regenerable from the `.icon`. 124 tests green (E2-231).

- **2026-08-29** — Second round on the same panel (owner: "the labels are too small"): every settings control label is now 15 pt rather than the Form's 13 pt. Getting there needed the same measure-don't-guess loop as the contrast fix, because a grouped `Form` on macOS resets the font environment for its own rows — `.font(.system(size: 14))` on the Form and `.dynamicTypeSize(.xLarge)` both rendered **pixel-identical** to the baseline snapshot, and `.controlSize(.large)` grew the toggles and popup buttons while leaving the text exactly where it was, costing vertical space for nothing. Only a font on the label's own `Text` takes, so the `Toggle`/`Picker`/`LabeledContent` call sites moved from the string-label convenience initialisers to explicit `label:` closures, and both styling rules now live in one private `View` extension (`settingsLabel()` / `settingsFooter()`) with the no-op findings written down next to them so nobody retries them. Values stay at 13 pt, which reads as ordinary label/value hierarchy. Known and unchanged: the panel is fixed at 320x480 and larger labels push slightly more of it below the fold — the same crowding already flagged on 2026-08-29. 124 tests green (E2-230).

- **2026-08-29** — Made the Settings panel readable (owner: "this menu is not readable"). Two separate causes, both measured against the offscreen `settings.png` snapshot rather than guessed at. **Contrast:** the grouped `Form` draws its own footers, so the hand-written `.font(.caption)` / `.foregroundStyle(.secondary)` on every footer were dead code — removing them changed the render by zero pixels. What the Form supplies is `#808080` on the panel, about 3.9:1, thin for a three-line paragraph. A `settingsFooter()` helper now sets `Color.primary.opacity(0.75)`, measured at `#5D5D5D` / ~6.4:1 in the re-rendered snapshot — still clearly subordinate to the near-black labels, so the hierarchy survives. Font size is not overridable here at all (the Form wins), which is why contrast was the only lever. **Copy:** the refresh footer read "Only while the list is open." — a fragment whose subject was the "Refresh every" picker two rows above, with a Docker toggle in between, so nothing on screen said what was only happening. Both footer sentences now name their own subject ("The list refreshes only while it's open.", "Docker integration names the container behind a published port…"). 124 tests green (E2-230).

- **2026-08-29** — The footer's refresh icon no longer strobes (owner: "it flickers every 2 seconds, it's disturbing"). `PortListModel.refresh()` set `isRefreshing` unconditionally and the polling loop called it on every tick, so the button swapped between the arrow glyph and a spinner — and changed width, shifting "6 of 15" — twice a second at the default interval. The scan body moved to a private `scan(showingProgress:)`; `refresh()` stays the public user-initiated entry and passes `true`, while the poll loop and the refreshes that follow a kill, a force kill or a container stop pass `false` (those rows already show their own progress). Nothing else changed: the poll still runs at the same cadence and still updates `listeners`, so the counts stay current — the half of the request a naive "poll less" fix would have broken. New test `backgroundPollingUpdatesTheListWithoutFlashingTheRefreshIndicator` uses `withObservationTracking` on `isRefreshing` to assert the flag never moves across a 1.2 s polling window while the list does update, then that a user `refresh()` does move it; proven able to fail by reverting the loop to `showingProgress: true`, which produced `** TEST FAILED **` on exactly that expectation. 124 tests green (E2-229).

- **2026-08-29** — Adopted **Liquid Glass** on macOS 26+, with every pre-Tahoe surface kept as the fallback below it; the deployment target stays 15.0, so both looks ship in one binary. All the `#available(macOS 26.0, *)` pairs live in one new file, `Squatter/Views/Glass.swift` (`glassSurface(in:interactive:fallback:)`, `glassButtonStyle()` / `chromeButtonStyle()` / `prominentButtonStyle(tint:)`, `softScrollEdges()`, a `GlassGroup` wrapper over `GlassEffectContainer`, and `LiquidGlass.isAvailable` / `.chipShape`), so no view branches on OS version inline. What changed: the popover no longer paints `.regularMaterial` on 26 — the menu bar panel already *is* glass, and the material was flattening it into the grey sheet the owner screenshotted; the filter field became a glass capsule; the footer's `.bar` and its hairline divider are gone in favour of `.buttonStyle(.glass)` chips sharing one container; the row hover chips (⋯ / ↗ / ✕) and the ⋯ menu chip became interactive glass sharing another; and the Kill / Force Kill / Stop confirmations use `.glassProminent` with a glass Cancel. Because glass bars are meant to be floated over rather than stacked above content, on 26 the filter bar and status bar are now `safeAreaInset`s and the list runs full height beneath them with `scrollEdgeEffectStyle(.soft, for: .vertical)`; below 26 they stay in the `VStack` where the opaque bars still cut cleanly. Verified visually against the real menu bar panel on macOS 27 — glass window, capsule field, glass footer and row chips all render. The safe-area/scroll-edge revision landed after that screenshot and is compile- and test-verified only; the display went to sleep before it could be re-shot, so the soft fade under the bars is unconfirmed by eye. 123 tests green, including the snapshot suite, which renders every list state offscreen and asserts its dimensions (E2-228).

- **2026-08-29** — **Released 0.2.0.** `scripts/release.sh --notarize` ran clean: both submissions Accepted, app stapled before the DMG was built, DMG stapled after. The decisive check — extracting the app *out* of the published DMG the way `brew` does — passes `stapler validate` and `spctl` (`accepted, source=Notarized Developer ID`) at 0.2.0/build 3. Tag `v0.2.0` pushed, GitHub Release published with `Squatter-0.2.0.dmg` (sha256 `7b6b7f35…`). **The cask-bump workflow then failed**, and the reason matters: `secrets.TAP_TOKEN` has never existed on this repo (`gh secret list` returns empty), so `GH_TOKEN` was the empty string and the push to the tap died with `Invalid username or token` / exit 128. The workflow has therefore never worked since it was added in `947e97c` — v0.1.2's cask was bumped by hand, which hid the gap. Bumped the tap manually instead: two-line change (version + sha256), same 2/2-lines-changed guard the workflow enforces, pushed to `homebrew-tap` main as a **verified-signature** commit (`483563c`), which the `protect-main` ruleset requires and accepted. Confirmed end to end with `brew fetch --cask National-Idea-LLC/tap/squatter` → `✔︎ Cask squatter (0.2.0)`, i.e. Homebrew downloaded the published DMG and its checksum matched. Creating the PAT is the owner's step and is now an open M3 box above; until then every release needs the manual bump.

- **2026-08-29** — Cut **0.2.0**: `MARKETING_VERSION` 0.1.2 → 0.2.0, `CURRENT_PROJECT_VERSION` 2 → 3, project regenerated from `project.yml`, and `CHANGELOG.md`'s `[Unreleased]` block moved under `## [0.2.0]` with a one-sentence summary. A minor rather than a patch because this is the first release with genuinely new capability since 0.1.0 — Docker awareness (naming containers and stopping them), the high-port rule, typed ignore entries, and Report a Bug. 0.1.1 and 0.1.2 were both fixes to how the app is signed and distributed rather than to the app itself. 123 tests green at the bump.

- **2026-08-29** — Bookkeeping sweep after reconciling `plans/`. Linear had drifted seven items behind the repo: the newest issue was E2-219 (plan 006), leaving plans 007-012 and the Settings reorder with `TRACKER.md` entries and no matching issue, against golden rule #8. Filed as E2-220 through E2-226, all **In Review** (never Done), milestone M3, and linked from the M3 task list above so the two sources agree. Both remaining manual checks confirmed by the owner and ticked — Launch at Login against the signed build, and stopping a real container through the Squatter UI, which was the last unverified link in the Docker chain (`docker stop` itself had been proven against a real daemon from the CLI on 2026-08-29, but never through the SwiftUI wiring). Also corrected a line that had gone stale: the Stop Container box claimed "`docker stop` has never run against a real daemon either", which the ticked box directly above it already contradicted. The four merged executor worktrees (the features batch plus plan 012) were pruned; `integration/features-007-011` is fully merged and left in place.

- **2026-08-29** — Plan 012: the snapshot suite (`SquatterTests/SnapshotTests.swift`) now asserts each render's actual dimensions, not just that the PNG exceeds 1 KB. `snapshot()` checks `host.bounds.size` against the requested `size` right after layout, then checks the bitmap's pixel dimensions against `size * window.backingScaleFactor` (derived from the window, never hard-coded 2x, so it holds on 1x CI runners too) — both failure messages name the snapshot. The settings snapshot now requests its real fixed 320x480 (`SettingsView.swift:133`) instead of the stale 320x560 nobody had checked. Before finalizing, the new assertion was proven able to fail: a temporary oversized canary (`Color.red.frame(width: 360, height: 2000)`) produced `** TEST FAILED **` with "canary escaped its frame: laid out 360.0x2000.0 pt, asked for 360.0x460.0 pt" and "canary rendered 720x4000 px, expected 720x920 px at 2.0x", then was reverted. Byte-count "looks blank" check kept as-is — it catches a different failure mode. Still 123 tests, zero failures (no new test cases, only new assertions inside the existing snapshot test).
- **2026-08-29** — Reordered the Settings form so the Ignored section precedes About, putting reference information (version, update check, Report a Bug, source link) last where scrolling costs nothing. Does **not** on its own bring the ignore-port field above the fold: the first three sections already fill the fixed 320x480 window, so Ignored moved from fifth to fourth and still needs a scroll. Fixing discoverability properly needs a taller window or merging "Hide high ports" into "Ignored" (both are row-hiding rules) — left as an open decision. Caught during this change: the settings snapshot test asserts only that the PNG exceeds 1 KB, so it passed against a completely broken layout (the Form closed early and `.formStyle`/`.frame` attached to the wrong view, rendering 320x3294 ungrouped). Only viewing the image caught it; the assertion needs to check dimensions, not bytes.
- **2026-08-29** — First real-daemon test of plans 007/008, against Docker Desktop 4.88.1 / engine 29.7.2 (see the ticked manual-check box above for the full readout). One real defect: `docker stop --time 5` is deprecated on this engine and prints a warning to stdout (`Flag --time has been deprecated, use --timeout instead`); `ContainerStopper.arguments(id:)` now uses `-t`, which has been valid since Docker 1.x and is the one spelling that works whether the user has an old or new engine. Also replaced the synthetic `docker-ps-sample.txt` fixture with captured real output: the synthetic version had an exposed-but-unpublished port (`443/tcp`) appended after a container's real publishes, which real `docker ps` never does — a container either publishes ports or shows its bare exposed ports, never both on the same line. The real fixture also swapped `api-db-1`'s image from the synthetic `postgres:16` to the real `redis:7-alpine` (one test assertion updated) and added a genuine UDP-only container and a genuine exposed-only container, so `unpublishedAndUdpPortsAreSkipped` now exercises real shapes. Everything else the parser assumed — `>` escaping, the `, ` separator, range syntax, host≠container ports, IPv6 addresses — matched the real daemon exactly, including the new `HealthStatus` string and nested `Platform` object fields, which `DockerPSLine` ignores as designed. Still 105 tests, zero failures.
- **2026-08-29** — Fixed the layout regression flagged in the entry below: the Stop Container confirmation button is now the bare verb **Stop**, matching the existing Kill/Force Kill pairing where the prompt line above already names the object. With "Cancel" + "Stop Container" the container name collapsed to a single truncated character and the "Stop this container?" prompt wrapped across four hyphenated lines — a user could not tell which container they were about to stop. With "Cancel" + "Stop", `api-db-1` renders in full and the prompt sits on two lines. Only the confirmation button changed; the ⋯ menu item, right-click menu item, and hover chip keep "Stop Container". `rules/ux-writing.md` updated to state the final shape. Still 105 tests (owner decision, no behavior change).
- **2026-08-29** — Container rows now offer Stop Container instead of Kill/Force Kill (plan 008, closing the P2 Docker spec item's remaining half). New `ContainerStopper` service runs `docker stop --time 5 <id>` — absolute path from `DockerProbe.findExecutable()`, never a shell — after validating the ID is lowercase hex, 12–64 characters (`ContainerStopper.isValidID`, the only argv built from parsed rather than constant input in this app); it never removes, force-kills, or restarts a container, and is deliberately a separate service from `ProcessKiller` so a subprocess never lives inside the type whose entire safety argument is "only `kill(2)` after re-validating the PID." `KillState` gained `.confirmingStop`/`.stopping`; `requestKill`/`requestForceKill` now refuse any row with `listener.container != nil` and `killSelected()` (⌫) routes container rows to Stop Container instead — a container row cannot reach `ProcessKiller` at all, verified by a dedicated test (`containerRowsCannotArmAProcessKill`). `rules/ux-writing.md` gained the Docker exception to "the destructive verb is Kill." 12 new tests (5 `ContainerStopperTests` + 7 `PortListModelTests`), 105 total. Unverified against a real Docker daemon, same as plan 007 — manual check added to the M3 list above. One layout observation for a reviewer: in the narrow 360 px snapshot canvas, the `list-confirm-stop` state truncates the container name harder than the pre-existing Force Kill case does (down to a single character) because "Cancel" + "Stop Container" is wider than "Cancel" + "Force Kill"; the buttons themselves stay fully readable, which is what the plan's done-criteria checks for, but the name truncation may be worth a follow-up polish pass.
- **2026-08-29** — Fixed a Docker-integration test isolation hazard from plan 007: `PortScanner`'s test call sites mostly omitted `docker:`, so on any machine that actually has Docker installed they fell through to the production default (`DockerProbe()`), which auto-detects the real CLI and spawns a live `docker ps` in the background. Since `Listener` is `Equatable` including `container`, a developer with anything published on port 3000 or 5432 would have seen `refreshLoadsListeners` and friends fail with a baffling diff — the suite only stayed green here because this machine has no Docker. Every test-constructed `PortScanner` now passes an explicit `docker:` (`nil` unless the test is specifically exercising annotation), so the suite is deterministic regardless of the host's Docker state. Production's default is unchanged. 93 tests, no count change (test-only fix).
- **2026-08-29** — Docker containers are now named instead of showing `com.docker.backend` (plan 007, read-only annotation half of the P2 spec item; "Stop container" instead of kill is plan 008). `PortScanner` gained an optional `DockerProbe` actor that runs `docker ps --no-trunc --format '{{json .}}'` (absolute path from a fixed allowlist covering Docker Desktop, Homebrew, Rancher Desktop and OrbStack — never a shell, never resolved via the shell's search path) and maps host ports to container name/image/id, matching by **host** port rather than process name so it works under OrbStack, Colima and Rancher Desktop too. Never blocks a scan: `snapshot()` returns the last cached mapping immediately and refreshes in the background every 5 s, backing off 30 s after a failed `docker ps` (daemon stopped is normal, not an error). A new "Docker integration" toggle in Settings (`squatter.dockerIntegration`, on by default) lets anyone opt out; with no Docker CLI installed, the probe never spawns a process at all — checked once at construction. `Listener` gained an optional `container: ContainerRef?` via a defaulted initializer parameter so every existing call site kept compiling untouched. Nobody on this project has verified this against a real Docker daemon (Docker isn't installed on the owner's machine) — everything is fixture-driven from the documented `docker ps` NDJSON format, including the `>` HTML-escaping of `->` that Go's JSON encoder produces and container/host port ranges (`9000-9002->9000-9002/tcp`). 9 new tests (7 `DockerProbeTests` + 1 `PortScannerTests` + 1 `PortListModelTests`), 93 total. Manual check against a running Docker Desktop is still open, above.
- **2026-08-29** — Fixed the "Hide ports above" field in Settings rendering as static text with no border, indistinguishable from a label — added `.textFieldStyle(.roundedBorder)` (plan 009 revision; nothing else about the field changed). Also known limitation surfaced while adding the section below: with the new "Hide high ports" section in place, the Settings window (fixed at 320×480, per `PROJECT_SPEC.md`) is now crowded enough that the "Ignored" section — including the new port-typing field added by plan 010 below — sits below the fold and needs a scroll to reach. Not fixed here; flagged for the owner to decide (taller window vs. a `ScrollView` indicator vs. reordering sections).
- **2026-08-29** — Added a way to type ports straight into the ignore list instead of only right-clicking a currently-visible row: a "Ignore these ports" field in Settings' Ignored section accepts ports separated by commas, spaces, tabs, semicolons or newlines (so a paste works as-is), and echoes back — rather than silently drops — any token that isn't a port from 1–65535 (`PortListModel.addIgnoredPorts(from:)` → `AddedPorts { added, skipped }`). The Ignored section itself is no longer conditional on having entries, since the empty state is exactly when a user most wants to add the first one. 7 new tests, 100 total (plan `010-type-ports-into-the-ignore-list.md`).
- **2026-08-29** — Added a "Hide high ports" setting: a toggle plus an editable threshold (default 10,000) that hides every listener above it, off by default so an update can't silently drop rows from someone's list. It's a new reason a row is hidden, not a new concept — `PortListModel.ignoreReason(_:)` replaces the old boolean `isIgnored` check and returns `.port`, `.processName`, or `.highPort`, with an explicit ignore-list entry always outranking the threshold so a manually-ignored port still offers Unignore. `.highPort` rows get their own undo (`stopHidingHighPorts()` / "Show Ports Above N" in the row menu) instead of Unignore, because removing a list entry can't reveal a row hidden by a rule — a dead menu item would be worse than none. 9 new tests, 93 total (plan `009-hide-ports-above-a-threshold.md`).
- **2026-08-29** — Settings' About section gets a "Report a Bug" button beside Check for Updates and View Source: it opens a new GitHub issue prefilled with the app version and macOS version (`SettingsModel.reportBug()`, built with `URLComponents` so both go through proper percent-encoding). Nothing else is collected — no logs, no listener list, no machine identifier — because the tracker is public and a bug report is not a diagnostic dump. 2 new tests, 86 total, including one that pins the body to contain no user name, home path, or the word "port".
- **2026-08-29** — Added `.github/workflows/cask.yml`: publishing a release now bumps the cask in `National-Idea-LLC/homebrew-tap` automatically, from the published asset, so the `version`/`sha256` pair cannot drift behind a release. It refuses to bump unless the app inside the DMG validates its own stapled ticket, reports `source=Notarized Developer ID`, and carries a bundle version matching the tag — the automated regression test for the v0.1.1 bug where only the DMG was stapled. Signing and notarization deliberately stay on the maintainer's Mac: putting the Developer ID certificate in GitHub secrets would let a GitHub compromise sign a malicious app, which is a far larger blast radius than this needs. Two mechanisms were verified rather than assumed: a commit pushed with a token is unsigned and would be rejected by the tap's signature rule, while a squash merge is signed by GitHub and is accepted; and `gh pr merge` refuses such a PR as `BLOCKED`, so the workflow calls the merge REST endpoint directly. Needs the `TAP_TOKEN` secret before it can run.
- **2026-08-29** — Security audit of the app itself: **no exploitable vulnerabilities**. Three concrete attack hypotheses were formed and all three disproved by testing rather than reading. Field injection into the `lsof -F` parser via a newline in a process name does not work — `lsof` escapes control characters in `-F` mode, verified by building a listener named `x<LF>p1<LF>cFAKE` and reading the raw output bytes. `proc_name` and `lsof +c0` agree exactly (both truncate at 31 characters), so the kill revalidation comment is accurate. URL scheme injection is unreachable because `localURLString` interpolates only a `UInt16` into a literal scheme. Two informational notes remain, both inherent to macOS: the unavoidable TOCTOU window between revalidation and `kill(2)`, and the 31-character name comparison. Audit artifacts: `~/security-audit-skill/squatter/run-1/`.
- **2026-08-29** — Deleted `Casks/squatter.rb` from this repository. Homebrew only reads a cask from a tap, so the copy in `National-Idea-LLC/homebrew-tap` was the only one that ever did anything; this one was a duplicate that had to be hand-edited on every release and would eventually have drifted. A stale `version`/`sha256` pair breaks `brew install` for everyone at once, so having one authoritative copy matters more than the convenience of keeping it beside the code. The tap repo is now the single source of truth, and the README says so (E2-219).
- **2026-08-29** — Hardened the release supply chain after a security audit of the Homebrew tap. The audit's one substantive finding: a Homebrew cask is executable Ruby, not metadata — confirmed by running a probe cask that executed arbitrary code on `brew info` alone — so write access to `National-Idea-LLC/homebrew-tap` is code execution as the user on every machine that installs or upgrades Squatter. Neither the `sha256` pin nor Developer ID signing constrains that path, because the code runs in Homebrew's process rather than in the signed app. Guarding it were no branch protections and a single admin account. Added a `protect-main` ruleset to both repos blocking force-push and branch deletion (no bypass, including for admins), registered an SSH signing key, and turned on signed commits. Org-wide 2FA enforcement is now on, restricted to secure methods only (SMS excluded, since SMS is defeated by a SIM swap). The ruleset was tested in both directions: a signed push is accepted, an unsigned one is refused with `GH013`. Audit artifacts: `~/security-audit-skill/homebrew-tap/run-1/`.
- **2026-08-29** — Released v0.1.2: `MARKETING_VERSION` 0.1.2, `CURRENT_PROJECT_VERSION` 2, `[Unreleased]` rolled into a dated `0.1.2` section. No app code changed since 0.1.1 — the release exists to ship a build whose *app bundle* carries a notarization ticket (E2-218), which only a rebuild can deliver. Verified by extracting the app out of the published DMG the way `brew` does: `stapler validate` passes and `spctl` reports `accepted, source=Notarized Developer ID`, where the same check on 0.1.1 reported no ticket. Cask bumped to 0.1.2 in both this repo and `National-Idea-LLC/homebrew-tap` (E2-219).
- **2026-08-29** — `scripts/release.sh` now notarizes and staples the app bundle before building the DMG, then staples the DMG as before. Previously only the DMG carried a ticket, so any install path that copies the app out of it — `brew install --cask`, or dragging to Applications and deleting the DMG — left an app that only passed Gatekeeper on a machine that could reach Apple's notary service; offline first launch would have been blocked. Verified against the shipped v0.1.1, where `stapler validate` on the installed app reported no ticket. Takes effect on the next release. (E2-218)
- **2026-08-29** — Fixed the deprecated `depends_on macos:` string form in `Casks/squatter.rb`, which made Homebrew warn on every operation. Also published `National-Idea-LLC/homebrew-tap`, the tap the README has advertised since v0.1.0 but which did not exist — `brew install --cask National-Idea-LLC/tap/squatter` failed for every user, since Homebrew refuses a cask outside a tap. Verified end to end: installs 0.1.1 to `/Applications`, `spctl` reports `accepted, source=Notarized Developer ID`. The cask is now duplicated in two repos and must be bumped in both on every release until the tag-triggered workflow automates it (E2-188). (E2-219)
- **2026-08-28** — Released v0.1.1: `MARKETING_VERSION` bumped, the `[Unreleased]` bullets rolled into a dated `0.1.1` section, and `Squatter.xcodeproj` regenerated so the new `project-sync` job stays green. Covers the Force Kill confirmation, the >64 KB pipe-drain freeze, and the stuck-scan watchdog (E2-208, E2-209, E2-210). Tagged `v0.1.1`, notarized and stapled DMG published to GitHub Releases, and `Casks/squatter.rb` bumped to 0.1.1 with the published asset's verified sha256.
- **2026-08-28** — Bumped `actions/checkout` from v4 to v5 in both CI jobs. GitHub had started forcing v4 onto Node 24 with a deprecation warning on every run, since v4 targets the retired Node 20 runtime.
- **2026-08-28** — Housekeeping alongside the four plans above: `.gitignore` now covers `.claude/` (agent worktrees and session state), and the `plans/` directory is committed as the record of the audit that produced them — including the four plan defects found during execution and the two audit conclusions the drain deadlock disproved.
- **2026-08-28** — `project.yml` is now enforced, not just declared: a `project-sync` CI job regenerates the project on every PR and fails if the committed `Squatter.xcodeproj` differs, and `scripts/release.sh` regenerates before archiving so a release can't be built from a hand-edited `.pbxproj`. Verified no existing drift against xcodegen 2.46.0 (E2-211).
- **2026-08-28** — Bounded the `lsof` subprocess: `ProcessRunner` now runs a 10-second watchdog that SIGTERMs (then SIGKILLs) a child that hasn't exited and reports `ScanError.timedOut`. Previously a child that never exited left the drains waiting on EOF forever, so `PortScanner.inFlight` was never cleared and every later scan — Refresh included — joined the same dead task, wedging the app until quit. 3 tests, 84 total (E2-210).
- **2026-08-28** — Split the subprocess mechanics out of `LsofRunner` into a `ProcessRunner` that takes its executable path and arguments at construction, leaving `LsofRunner` as the fixed-command wrapper so golden rule #7 still reads the same, and regenerated `Squatter.xcodeproj` to register the new test file. Writing the large-output test exposed a real deadlock: `FileHandle.bytes` serialises reads across handles, so the `async let` drain was never actually concurrent and any child writing more than the 64 KB pipe buffer to stdout hung forever — measured, 128 KB never returned. Each pipe is now drained by a blocking read on its own thread (4 MB in ~6 ms). 7 tests, 81 total (E2-209).
- **2026-08-28** — Closed the last unconfirmed kill path: the row menu's Force Kill armed nothing and sent SIGKILL on click, so it now arms its own `KillState.confirmingForce` ("Force kill this process?" with Cancel / Force Kill), Escape and a new arming both clear it, and `PortListModel.kill`/`forceKill` refuse rows the user doesn't own instead of relying on the view's `.disabled`. Armed prompts are also dropped when the popover closes. 5 tests, 74 total (E2-208).
- **2026-08-28** — Row action chips (⋯ / ↗ / ✕) redrawn on `.thickMaterial` with tinted glyphs so they stay legible over the window background, the hover fill and the accent-coloured selection alike; snapshot windows are now made key so selection and prominent buttons render in colour instead of the inactive grey that was hiding these contrast problems (E2-200).
- **2026-08-28** — Only one kill confirmation can be armed at a time: arming a row disarms any other pending one, while kills already in flight (terminating / still running / failed) are left alone; 2 tests, 69 total (E2-197).
- **2026-08-28** — Fixed truncated confirmation copy: the prompt moved to the row's second line ("Kill this process?" in red) so the process name keeps its usual width, and the Cancel/Kill and Force Kill controls are `fixedSize` with layout priority so they can never collapse into ellipses. Snapshot now arms the longest name in the fixture (E2-197).
- **2026-08-28** — Killing now confirms first (owner decision, overrides the spec's fast-path design): ✕, the Kill Process menu item, ⌫ and the accessibility action all arm `KillState.confirming`; the row shows "Kill <process>?" with Kill / Cancel, Escape cancels every armed row, a refresh that drops the row clears it, and other users' rows can't be armed. Spec interaction details updated; 4 new tests, 67 total (E2-197).
- **2026-08-28** — Hovered rows now draw a subtle background so it is clear which row the ⋯ / ↗ / ✕ buttons belong to; selection keeps its own highlight, and the fade is skipped under Reduce Motion (E2-196).
- **2026-08-28** — v0.1.0 notarized: submission 20738e21 Accepted, stapled, `spctl` accepts (`source=Notarized Developer ID`). DMG sha256 `82b34bd3…9b18249a` wired into the cask; CHANGELOG cut to `[0.1.0]`; README screenshots and install instructions added (E2-188).
- **2026-08-28** — Release pipeline: app icon generated from `scripts/make-icon.swift` (dark panel, one lit LED), `scripts/release.sh` (test → archive → Developer ID export → signature/hardened-runtime/sandbox checks → signed DMG → optional notarize+staple), `Casks/squatter.rb` skeleton. Verified end to end: `build/Squatter-0.1.0.dmg`, 2.0 MB, signed by M8A3G95883, hardened runtime on; `spctl` rejects only as "Unnotarized Developer ID" (E2-188).
- **2026-08-28** — Renamed OpenPorts → **Squatter** (owner decision, spec Q#5: OpenPorts collided with openports.app). Xcode targets, bundle IDs `sa.ni.squatter`, `squatter.*` defaults keys (no migration — nothing shipped), UI strings, About URLs, CI, docs and rules; GitHub repo and Linear project renamed. Spec Q#6 resolved: GitHub Releases **and** a Homebrew cask at launch. 63 tests green (E2-187).
- **2026-08-28** — Row options button: a ⋯ menu in the hover actions presents the same items as the right-click menu, built from one shared `menuItems` builder so they can't drift (E2-186).
- **2026-08-28** — Popover redesigned (owner: "look ugly"): per-row status LED (green yours / gray other users / amber terminating / red still running), rows grouped Yours / Other users / Ignored via `PortListModel.groups`, port number as the dominant monospaced column, bind-address chips with network-reachability tooltip, tinted square hover actions, rounded filter field, material background, `.bar` status bar, "unlit panel" state views; dark-mode snapshot added; 63 tests (E2-185).
- **2026-08-28** — Settings About section: version from the bundle, "Check for Updates" → GitHub Releases, "View Source" → repo (both via `SystemActions`, no network in-app); settings popover fixed at 320×480 and scrolls. 1 test, 62 total. **M2 tasks complete** (E2-184).
- **2026-08-28** — Menu bar count badge: "Show count in menu bar" toggle (`squatter.showCountInMenuBar`), `Label` count next to the icon, single polling loop that runs at the popover interval while open and every 10 s in the background only while the badge is on; count excludes ignored rows; 4 tests, 61 total (E2-183).
- **2026-08-28** — Keyboard navigation: list takes focus on open; ⏎ opens, ⌫ kills (own processes only, not while a kill is in flight), ⌘C copies the URL — via `openSelected`/`killSelected`/`copySelectedURL` model intents; 2 tests, 57 total (E2-182).
- **2026-08-28** — Sort order: `SortOrder` (port | processName) persisted as `squatter.sortOrder`, "Sort by" picker in Settings, case-insensitive name sort with port/PID tiebreak; 2 tests, 55 total (E2-181).
- **2026-08-28** — M2 started; task list E2-180…E2-184. Ignore list shipped: context-menu Ignore Port / Ignore <process> / Unignore, persisted via `Preferences`, "N hidden · Show" footer toggle, dimmed ignored rows with eye.slash, "Everything is ignored." state, Ignored section in Settings with remove buttons; 5 tests (E2-180).
- **2026-08-28** — Launch at Login via `SMAppService` behind `LoginItemManaging`; `SettingsModel` + `SettingsView` popover from the footer gear (⌘,): login toggle with approval/error handling, refresh interval 1/2/5 s applied on the next tick. 5 model tests + settings snapshot; 48 total. **M1 tasks complete** (E2-179).
- **2026-08-28** — Popover UI: `PortListView` (filter, list with selection, ⏎ opens, footer refresh/count/quit, loading/empty/filter-empty/error states, stale-list error banner) and `PortRow` (hover ↗/✕, greyed other-user rows, inline Killing…/Still running → Force Kill/error, context menu, accessibility actions). Offscreen snapshot test renders all states to PNG (E2-176).
- **2026-08-28** — `PortListModel` (`@MainActor @Observable`): start/stop polling, refresh with last-good list + error, filter over port/name/PID, kill → 2 s grace → Force Kill offer, open/copy via `SystemActions` (AppKit behind a protocol); `Preferences` + `DefaultsKeys`; shared test doubles; 12 new tests, 42 total (E2-169).
- **2026-08-27** — `ProcessKiller`: `kill(2)` SIGTERM/SIGKILL guarded by a `proc_name` re-check of the PID (verified `proc_name` == `lsof +c0` for live processes), typed `KillError`, `waitForExit` poll for the Force Kill offer; 9 tests incl. a real child-process kill (E2-168).
- **2026-08-27** — `LsofRunner` (absolute-path `Process`, no shell, concurrent pipe drain) and `PortScanner` actor (single in-flight scan, callers coalesce) with typed `ScanError`; 9 tests incl. coalescing and a real-`lsof` integration check (E2-166).
- **2026-08-27** — `LsofParser` implemented: pure `String -> [Listener]`, `(pid, port)` merge with address union, bracketed IPv6, injectable UID→name resolver; 10 parser tests incl. the live fixture (E2-164). M1 task list added.
- **2026-08-27** — CI added: GitHub Actions runs `xcodebuild test` on every PR and push to `main` (E2-145). M0 tasks all complete.
- **2026-08-27** — Xcode project scaffolded (`project.yml` → xcodegen): `Squatter` app + `SquatterTests`; `MenuBarExtra` stub, `Listener` model, live `lsof` fixture + 2 passing Swift Testing tests. Build and test verified (E2-143, E2-144).
- **2026-08-27** — Spec open questions #1–#3 resolved by owner: "player" = app; minimum macOS 15; row click selects (browser opens via ↗ / ⏎ / context menu). Spec flows and interaction details updated (E2-142).
- **2026-08-27** — `git init`, first commit, public GitHub repo created under National-Idea-LLC; MIT `LICENSE` and `README.md` added; spec open question #7 resolved (MIT).
- **2026-08-26** — Linear project [Squatter](https://linear.app/ielyas/project/squatter-8016284756f8) created (team Elyas) with milestones M0–M3 and issues E2-140…E2-145; sync rule added as golden rule #8 + `rules/issue-tracker-status.md`.
- **2026-08-26** — Project rules and agent files bootstrapped (AGENTS.md, CLAUDE.md, rules/, TRACKER.md, CHANGELOG.md, .gitignore).
- **2026-08-26** — `PROJECT_SPEC.md` generated from portmanager.app / squatter.app research and a verified `lsof -F` output sample.
